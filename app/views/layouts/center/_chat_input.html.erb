<%= form_with url: messages_path(chat_id: @chat.id), local: true, data: { turbo: false } do |f| %>
  <%= f.text_area :content, name: 'message[content]' %>
  <%= f.hidden_field :chat_id, value: @chat.id, name: 'message[chat_id]' %>
  <%= f.submit "送信" %>
<% end %>
