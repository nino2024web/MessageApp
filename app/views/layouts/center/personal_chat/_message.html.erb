<div class="message <%= message.user_id == current_user_id ? 'own-message' : 'other-message' %>">
  <p class="content"><%= message.content %></p>
  <p class="timestamp"><%= message.created_at.strftime("%Y/%m/%d %H:%M") %></p>
</div>
