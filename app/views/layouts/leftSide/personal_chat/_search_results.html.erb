<!-- 検索結果 -->
<div id="search-results">
  <% if params[:name].present? %>
    <% if search_results.any? %>
      <ul class="search-results">
        <% search_results.each do |user| %>
          <li class="search-item">
            <span class="user-name"><%= user.name %></span>
            <% if current_user.friends.include?(user) %>
              <%= button_to "チャット開始", start_chat_user_path(user), method: :post, data: { turbo: false } %>
            <% end %>
            <div id="friend-request-btn-<%= user.id %>" data-controller="friend-requests" data-friend-requests-user-id-value="<%= user.id %>">
              <%= render "layouts/leftSide/personal_chat/request_button", user: user %>
            </div>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p class="no-chat-message">該当するユーザーが見つかりませんでした。</p>
    <% end %>
  <% else %>
    <p class="no-chat-message">名前を入力してください。</p>
  <% end %>
</div>
